requests==2.31.0
beautifulsoup4==4.12.2
supabase==2.3.0
python-dotenv==1.0.0
lxml==5.1.0

# Official TNPSC PDF OCR pipeline (tnpsc_official_scraper.py)
PyMuPDF==1.24.9      # render scanned PDF pages to images
Pillow==10.4.0       # image handling for the Tesseract fallback
pytesseract==0.3.13  # optional local OCR fallback (needs Tesseract + 'tam')
# Cloud OCR (recommended for Tamil) uses Google Cloud Vision over REST via
# `requests` + a GOOGLE_VISION_API_KEY — no extra SDK required.

# Bilingual pairing: machine-translate EN questions -> Tamil _ta columns
# (translate_to_tamil.py). Free Google MT endpoint, no key needed.
deep-translator==1.11.4

# AI explanation generation (generate_explanations.py) — fills missing/
# placeholder explanations using Claude, anchored on the verified answer.
anthropic>=0.49.0
